# Copy this file to .env and fill in your own values.
# .env is git-ignored and must NEVER be committed.

NINJASUITE_EMAIL=tu-email@example.com
NINJASUITE_PASSWORD=tu-password-aqui

# Optional: default target URL if you don't pass one as a CLI argument
TARGET_URL=
